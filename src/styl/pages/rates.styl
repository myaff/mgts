.rates {
  .section {
    &__content {
      max-width: 1160px;
      @media $display-breakpoints.sm-and-down {
        padding-top: 40px;
        padding-bottom: 20px;
      }
    }
  }

  &__list {
    flexy(space-between, stretch);
    margin: 60px auto 120px;

    @media $display-breakpoints.lg-and-down {
      margin: vw(60) auto vw(120);
    }
    @media $display-breakpoints.md-and-down {
      margin: 33px auto 70px;
    }
    @media $display-breakpoints.sm-and-down {
      display: block;
      margin: vmin(30, 320) 0;
    }
  }
  &__item {
    flex: 0 0 calc(50% - 20px);
    // ie11 fix
    max-width: calc(50% - 20px);
    // - 

    @media $display-breakpoints.md-and-down {
      flex-basis: calc(50% - 15px);
    }
    @media $display-breakpoints.sm-and-down {
      margin: vmin(20, 320) auto;
      max-width: 100%;
    }
  }
}

.rate-card {
  padding: 40px;
  background: url('../img/rate.png') center no-repeat;
  background-size: cover;
  border-radius: 35px;

  @media $display-breakpoints.lg-and-down {
    padding: vw(40);
  }
  @media $display-breakpoints.md-and-down {
    padding: 25px 30px 30px;
    border-radius: 25px;
  }
  @media $display-breakpoints.sm-and-down {
    padding: vmin(20, 320);
  }

  &__info {
    margin: 20px 0;
    flexy(space-between, center);

    @media $display-breakpoints.sm-and-down {
      margin: vmin(20, 320) 0;
    }
  }
  &__desc {
    flex: 1 1 auto;

    .desc,
    .option {
      font: 21px/1 $font-base;
      letter-spacing: 0.05em;
      white-space: nowrap;

      @media $display-breakpoints.lg-and-down {
        font-size: vw(21);
      }
      @media $display-breakpoints.md-and-down {
        font-size: 12px;
      }
      @media $display-breakpoints.sm-and-down {
        font-size: vmin(12, 320);
      }
    }
    .speed {
      font: bold 32px/1 $font-base;
      letter-spacing: 0.05em;
      margin: 5px 0;

      @media $display-breakpoints.lg-and-down {
        font-size: vw(32);
      }
      @media $display-breakpoints.md-and-down {
        font-size: 18px;
      }
      @media $display-breakpoints.sm-and-down {
        font-size: vmin(18, 320);
      }
    }
  }
  &__price {
    flex: 0 0 190px;
    margin-left: 10px;
    flexy(center, flex-start, wrap, column);
    height: 80px;

    @media $display-breakpoints.lg-and-down {
      height: vw(80);
      flex-basis: vw(190);
    }
    @media $display-breakpoints.md-and-down {
      height: 40px;
    }
    @media $display-breakpoints.sm-and-down {
      height: vmin(40, 320);
      flex-basis: vmin(90, 320);
    }

    .price {
      font: bold 80px/1 $font-base;
      margin-right: 5px;

      @media $display-breakpoints.lg-and-down {
        font-size: vw(80);
      }
      @media $display-breakpoints.md-and-down {
        font-size: 40px;
      }
      @media $display-breakpoints.sm-and-down {
        font-size: vmin(40, 320);
      }
    }
    .cur,
    .per {
      font: 32px/1 $font-base;
      letter-spacing: 0.05em;

      @media $display-breakpoints.lg-and-down {
        font-size: vw(32);
      }
      @media $display-breakpoints.md-and-down {
        font-size: 14px;
      }
      @media $display-breakpoints.sm-and-down {
        font-size: vmin(14, 320);
      }
    }
  }

  &__btn {
    text-align: center;

    @media $display-breakpoints.sm-and-down {
      margin-top: vmin(40, 320);
    }
  }
}